{
  "name": "backend",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "dev": "nodemon",
    "start": "npm run build && node dist/index.js",
    "build": "rimraf dist && tsc",
    "check-types": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.9.0",
    "rimraf": "^6.0.1",
    "ts-node": "^10.9.2",
    "typescript": "^5.6.3"
  },
  "dependencies": {
    "shared": "*",
    "socket.io": "^4.8.1"
  }
}
